import Logo from "../components/Logo";
import Resetpassword from "../components/Auth/Resetpassword";

const ResetPassword = () => {
  return (
    <div className="welcome resetpassword">
      <Logo />
      <Resetpassword />
    </div>
  );
};

export default ResetPassword;
